#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <time.h>

#define randnum(min, max) \
        ((rand() % (int)(((max) + 1) - (min))) + (min))

int main() {

	int t[3];
}